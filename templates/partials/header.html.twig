<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="collapse navbar-collapse" id="navbarColor01">
            <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('home') }}">Accueil <span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('qualification') }}"><i class="fas fa-graduation-cap"></i> Qualifications</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('chantiers') }}"><i class="far fa-building"></i> Chantiers réalisés</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('devis') }}"><i class="fas fa-hand-holding-usd"></i> Devis</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('contact') }}"><i class="far fa-envelope"></i> Contact</a>
            </li>
            </ul>
        </div>
        <div>
            <ul class="navbar-nav mr-auto">
                 <li class="nav-item">
                    <a class="nav-link" href="{{ path('account_registration') }}" id="inscription"><i class="fas fa-door-open black"></i> Inscription</a>
                 </li>
                 <li class="nav-item">
                    <a class="nav-link" href="{{ path('account_login') }}" id="connexion"><i class="far fa-user"></i> Connexion</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('account_logout') }}" id="deconnexion"><i class="fas fa-sign-out-alt black"></i> Déconnexion</a>
                </li>
                 <li class="nav-item">
                    <a class="nav-link" href="{{ path('account_admin') }}" id="admin"><i class=""></i> Administration</a>
                </li>
            </ul>
        </div>
</nav>
{% for message in app.flashes('success') %}
            <div class="alert alert-success">
                {{ message }}
            </div>
        {% endfor %}
 {% if app.user %}
            <script>document.getElementById('inscription').style.display ='none';</script>
            <script>document.getElementById('connexion').style.display ='none';</script>
            <script>document.getElementById('deconnexion').style.display ='block';</script>
            <script>document.getElementById('admin').style.display ='none';</script> 
          
        {% else %}
            <script>document.getElementById('inscription').style.display ='block';</script>
            <script>document.getElementById('connexion').style.display ='block';</script>
            <script>document.getElementById('admin').style.display ='none';</script> 
            <script>document.getElementById('deconnexion').style.display ='none';</script>

{% endif %}

{% if is_granted('ROLE_ADMIN') %}
    <script>document.getElementById('admin').style.display ='block';</script>
{% endif %}
  
        